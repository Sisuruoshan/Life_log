rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Only allow authenticated users to read/write their own files or all files
      allow read, write: if request.auth != null;
    }
  }
}
